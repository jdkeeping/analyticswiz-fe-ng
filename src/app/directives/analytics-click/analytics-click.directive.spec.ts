import { AnalyticsClickDirective } from './analytics-click.directive';

describe('AnalyticsClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AnalyticsClickDirective();
    expect(directive).toBeTruthy();
  });
});
