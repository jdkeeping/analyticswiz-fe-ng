import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.baystatehealth.mybaystatehealth',
  appName: 'mybaystate',
  webDir: 'www'
};

export default config;
